# ============================================================
# Crystal Presentes — Variáveis de Ambiente
# Copie para .env e preencha os valores reais
# NUNCA commitar o .env no GitHub
# ============================================================

# Supabase
SUPABASE_URL=https://qltfwxmflygyrobvwqxw.supabase.co
SUPABASE_ANON_KEY=sb_publishable_PtCxpU9f3M70teEyfS8u3A_dxSMeYi0
SUPABASE_SERVICE_KEY=            # Project Settings → API → service_role (secret)

# Admin do Painel
ADMIN_EMAIL=admin@crystalpresentes.com.br
ADMIN_PASSWORD_HASH=             # gerar com: python -c "from passlib.hash import bcrypt; print(bcrypt.hash('sua_senha'))"

# Sessão JWT
SECRET_KEY=                      # gerar com: python -c "import secrets; print(secrets.token_hex(32))"
ACCESS_TOKEN_EXPIRE_MINUTES=480  # 8 horas

# App
APP_ENV=production               # development | production
APP_HOST=0.0.0.0
APP_PORT=8001                    # diferente do MK Report (8000)
